<div id="<%= dom_id task %>">
	<div class="mt-2 flex justify-between">
		<div class="flex">
			<%= task.name %>
			<div class="font-bold ml-5 text-blue-500">
        <%= task.finish_at.to_date %>
			</div>
			<%= link_to "Ver", task, class: "rounded-lg ml-5 py-2 px-4 bg-gray-50 inline-block font-small" %>
			<hr class="mt-6">
		</div>
	</div>
</div>