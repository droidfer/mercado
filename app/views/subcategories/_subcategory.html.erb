<div id="<%= dom_id subcategory %>">
  <div class="flex justify-between items-center mt-2">
    <%= link_to "#{subcategory.name} / #{subcategory.category.name} ", subcategory_path(subcategory) %>
  
    <% if action_name != "show" %>
      <div class="flex">
      <%= link_to "Ver", subcategory, class: "rounded-lg py-1 px-2 bg-gray-50 inline-block" %>
      <%= link_to 'Editar', edit_subcategory_path(subcategory), class: "rounded-lg py-1 ml-2 px-2 bg-red-50 inline-block" %>
      </div>
    <% end %>
    
  </div>
  
  <hr class="mt-2">

</div>
